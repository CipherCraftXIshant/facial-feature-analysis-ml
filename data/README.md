# Data Directory

This directory stores datasets for the facial feature analysis project.

- `raw/`: Raw downloaded datasets (e.g. UTKFace dataset downloaded via Kaggle API). Excluded from version control via `.gitignore`.
- `processed/`: Processed datasets, features, and transformed data ready for model ingestion.
